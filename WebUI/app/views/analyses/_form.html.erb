<%= form_with(model: analysis) do |form| %>
  <%= form.hidden_field :areas_of_interest, value: '' %>

  <% if analysis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(analysis.errors.count, "error") %> prohibited this analysis from being saved:</h2>

      <ul>
        <% analysis.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-success btn-sm'  %> <%= link_to 'Cancel', analyses_path, method: :get, class: 'btn btn-default btn-sm'  %>
  </div>
<% end %>
